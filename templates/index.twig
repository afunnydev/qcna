{#
 |------------------------------------------------------------------------------
 | Index
 |------------------------------------------------------------------------------
#}

{% extends '_layouts/default' %}

{% block page %}
    {# Include the appropriate template for the given section handle #}
    {% include [
        '_entries/' ~ entry.section.handle ~ '/' ~ entry.type,
        '_entries/' ~ entry.section.handle ~ '/default',
        '_entries/default'
    ] %}
{% endblock %}